# plant-disease-detection
Mobile-first AI plant disease detection prototype using FastAPI, PyTorch, and OpenCV. It captures/uploads leaf images, validates leaf presence, segments up to 5 leaves, classifies each as Crop+Disease, applies confidence thresholds, generates Grad-CAM heatmaps, and returns   explainable results with logging/analytics via a responsive web UI

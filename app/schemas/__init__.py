"""Pydantic schema package for HTTP request and response models.

These schemas define the contract between the FastAPI backend and the browser
client. They centralize payload structure so the API layer, service layer, and
future maintainers share one clear definition of request/response fields.
"""
